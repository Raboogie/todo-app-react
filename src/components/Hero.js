import hero from "../todo-hero.jpg"

const Hero = () => {
    return (
        <>
            <section className="hero">
                <img src={hero} alt=""/>
            </section>
        </>
    )
}

export default Hero;